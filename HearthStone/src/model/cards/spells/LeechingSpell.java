package model.cards.spells;

public interface LeechingSpell {
	//Interface containing the methods available to all leeching spells, i.e. that damage the
	//opponent’s while restoring the hero’s own health.
}
